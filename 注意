中文无法保存
FCB中增加创建者ID
权限linux简化，2为写权限，4为读权限，第一位表示创建者权限，第二位表示其他用户权限，默认权限为64，暂时不增加修改功能
设置4个用户，id0为管理员，1，2，3，增加页面选择使用用户，不设密码
editview中configureTextPane(String content)，没有增加0权限，表示不可读，
还需增加删除权限
	if(MainController.currentID!=fcb.CreateID||MainController.currentID!=0){
						System.out.println("权限不足");
						JOptionPane.showMessageDialog(MainController.this.view,"权限不足");
					}else {